---
sidebar_position: 1
---

export const Highlight = ({children, color, black}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '5px',
      color: black ? '#000' : '#fff',
      padding: '7px',
      cursor: 'pointer',
    }}>
    {children}
  </span>
);

## まずはこれ

:::info
    詳しいことは作成者かGithub見てください
:::

## これがAPIDocsです

すごいでしょ？これがAPIDocsらしいですよ

## BaseURI

```
/
```

:::warning

そのうち変わるかもしれませんが、やる気がないので変わりません。

:::

