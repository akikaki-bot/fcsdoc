---
sidebar_position: 2
---

export const Highlight = ({children, color, black}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '5px',
      color: black ? '#000' : '#fff',
      padding: '7px',
      cursor: 'pointer',
    }}>
    {children}
  </span>
);

```
/stock
```

> <Highlight color="#00ccff"> GET </Highlight>

> <Highlight color="#c1c10f"> Object </Highlight>

> <Highlight color="#ff660f"> Array </Highlight>

## Properties

| 値 | 型 | いつももあるか | 説明 |
| --- | --- | --- | --- |
| data | StockData[] | YES | Array of StockData Object |